workflows:
  android-webview-release:
    name: Build Android WebView APK
    environment:
      android:
        image: android-31
      java: 11
    scripts:
      - echo "Building APK..."
      - ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk
